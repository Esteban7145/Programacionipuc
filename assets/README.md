Coloca aquí el archivo original del logo oficial sin editar con este nombre exacto:

- `logo-subido.png`

La aplicación pública (`/`) y el panel privado (`/admin`) lo cargarán directamente desde esa ruta.
